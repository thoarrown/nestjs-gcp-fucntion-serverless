/**
 * Type of application operating modes, defined as: poduction, development, notifications, test
 * @example production
 */
export enum ConfigMode {
  'production' = 'production',
  'development' = 'development',
  'notifications' = 'notifications',
  'test' = 'test',
}
